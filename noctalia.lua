-- Generated by Noctalia

local primary = "rgb(84d2e5)"
local surface = "rgb(0f1416)"
local on_surface = "rgb(dee3e5)"
local secondary = "rgb(b2cbd2)"
local on_secondary = "rgb(1c3439)"
local error = "rgb(ffb4ab)"
local on_error = "rgb(690005)"

local function apply_theme()
    hl.config({
        group = {
            col = {
                border_active = secondary,
                border_inactive = surface,
                border_locked_active = error,
                border_locked_inactive = surface,
            },

            groupbar = {
                col = {
                    active = secondary,
                    inactive = surface,
                    locked_active = error,
                    locked_inactive = surface,
                },
                text_color = on_secondary,
                text_color_inactive = on_surface,
                text_color_locked_active = on_error,
                text_color_locked_inactive = on_surface,
            },
        },
    })
end

return {
    colors = {
        primary = primary,
        surface = surface,
        on_surface = on_surface,
        secondary = secondary,
        on_secondary = on_secondary,
        error = error,
        on_error = on_error,
    },
    apply_theme = apply_theme
}
